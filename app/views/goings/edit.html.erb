<div class="row form">
  <div class="col-lg-12">
    <%= form_for(@going) do |f| %>
    <h3><span>進捗</span><%= @going.text %></h3>
      <div class="form-group">
        <%= f.text_area :text, placeholder: "編集してください", cols:"30", rows:"10", autofocus:"true", class:"form-control" %>
      </div>
      <%= f.submit "更新する", class:"btn btn-primary withripple"%>
    <% end %>
  </div>
</div>